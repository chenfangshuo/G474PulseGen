# 贡献指南

感谢你有兴趣参与。这个项目是**工程样板的固件**，不是成熟产品——所以下面有些约定比较硬，都是踩过坑之后定下来的，请先读完再动手。

## 先确认你改的是哪一层

| 层次 | 内容 | 能否接受外部贡献 |
|:---|:---|:---|
| **自有业务代码** | `Pulse.c` / `uart_comm.c` / `Preset.c` / `WouoUI_user.c` / `Key.c` / `pc_host/` | ✅ 欢迎 |
| **文档** | `README.md` / `HARDWARE.md` | ✅ 尤其欢迎——发现文档与代码不符请务必提 |
| **CubeMX 生成区** | `Core/Src/*.c` 中 `USER CODE` 保护块**之外**的部分、`Drivers/` | ❌ 不要直接改，见下 |
| **第三方库** | `WouoUI*.c/h`、`OLED*.c/h` | ⚠️ 可以改，但要登记修改记录，见下 |

---

## 一、开发环境

见 [`README.md` §9](README.md)。一句话：装 **STM32CubeCLT**（含 GCC / CMake / Ninja），然后

```bash
cmake --preset Debug && cmake --build build/Debug
```

**编译必须零告警。** 本工程开了 `-Wall -Wextra`，但刻意没加 `-Werror`——所以告警不会挡住构建，得你自己看。

---

## 二、代码规范

### 语言与命名

- **注释一律用简体中文。** 全工程都是中文注释，混进英文注释会破坏一致性。
- **一律用标准整型**：`uint8_t` / `uint16_t` / `uint32_t` / `int32_t`，不要用 `int` / `unsigned` 之类宽度不定的类型（绘图 API 等第三方接口除外）。
- **引脚禁止裸写编号。** 业务逻辑里不出现 `GPIO_PIN_12` / `GPIOA` 这种字面量，一律用头文件里语义化的宏（`LOADSW_PIN` / `HRT_CHB2_Pin` / `KEY_TRG_Pin`）。新增引脚请先在对应头文件里定义宏，再使用。

### `HARDWARE.md` 是唯一基准

根目录 [`HARDWARE.md`](HARDWARE.md) 是引脚、外设映射、电气有效电平的**单一事实源**。任何涉及引脚的改动，必须与它一致——**不允许凭推测或"通用例程默认值"来定引脚**。

如果你发现代码与 `HARDWARE.md` 不符，**多半是文档或代码有一方错了，请提 issue 说明**，不要默默改一边。

### 寄存器直操作必须带注释

只要直接写寄存器（`HRTIMx->sTimerxRegs`、`TIMx->CCR1`、`GPIOx->BSRR` …），**必须附一行中文注释说明计数值与时序含义**。例如：

```c
/* CMP4 = 周期值 - 1: 每周期末尾触发一次中断, 用于软件重发下一个脉冲。
 * ±1 就会漏触发或多触发, 不要"顺手"改成 period_value。 */
HRTIM1->sTimerxRegs[0].CMP4xR = period_value - 1U;
```

理由很实际：这类代码单看数字无法审阅，而改错一位的后果可能是打出畸形脉冲甚至上下桥直通。

---

## 三、动手前必须知道的红线

**这是一块 ns 级时序敏感的发波板，有些代码"看起来能简化"但绝对不能动。** 改之前请先读 [`README.md` §6](README.md)（防炸机与安全逻辑）。

### 1. 时序补偿与对齐

- `Pulse_TriggerFireAll()` 用**单次 CR2 写**同时置 `TCRST` 与 `TARST/TBRST/TDRST`。拆成两次写会引入 ≥1 个 APB 周期偏移，SYNC 与首脉冲的 ns 级对齐直接失效。
- `NPULSE_INTERVAL_COMP_US`（`Pulse.c`）是**实测标定**值，且**与编译优化等级耦合**——它补偿的是中断响应延迟。**改优化等级或升级工具链后必须重新标定**，方法见 README §4.4。
- 各种分频表、阈值表、钳位边界（`96 ~ 65503`）都是实测标定出来的，不要"取整"或"简化"。

### 2. 硬件保护

- `Pulse_EmergencyStop()` 的**五步关断顺序**是刻意排的：`LOADSW_DISABLE()` 必须是第一句且零依赖。不要重排、不要"优化"。
- `Pulse_Fault_Init()` 的 PA15 极性（低有效）与 `Filter=2` 档位，与 `HARDWARE.md` §5.3 严格对应，**不得改动**。
- 涉及互补输出的任何改动，**第一原则是防上下桥直通**。死区由 HRTIM 硬件发生器插入，不要改成软件延时。

### 3. `volatile` 与 ISR 签名

- 17 组跨中断通信变量的 `volatile` **必须逐字保留**。本工程 `-O3`，去掉 `volatile` 会被优化掉，表现为「Fault 后不关断」「按键无响应」「串口丢字节」——而且**编译不会有任何提示**。
- ISR 与 HAL 回调的函数签名不要改（改了必然编译失败或丢保护），包括 `stm32g4xx_it.c` 里的向量入口。

### 4. CubeMX 保护块

**自定义代码只能写在 `/* USER CODE BEGIN xxx */` 与 `/* USER CODE END xxx */` 之间。** 保护区之外是生成区，改了会在下次 CubeMX 重新生成时**静默丢失**。

需要改外设配置（时钟、引脚、外设开关）时，请改 `.ioc` 文件后重新生成，**不要手改生成代码**。完整流程见 [`README.md` §9.5](README.md)。

> 特别提醒：本项目有一条 `cmake/check_regen_invariants.cmake` 构建前置检查（14 项）。它存在的理由是一次真实事故——重新生成把 `TICK_INT_PRIORITY` 从 0 静默改回 15，**编译通过、运行正常**，只是悄悄恢复了 ISR 死锁的前提条件。如果你的 PR 触发了 FATAL 项，说明该配置应该写进 `.ioc` 而不是手改源码。

### 5. 第三方库的修改要登记

`WouoUI*.c/h` 是 MPL-2.0（详见 [`LICENSE-MPL-2.0`](LICENSE-MPL-2.0)）。按该许可 §3.4，修改需要作出说明——请在**文件头的修改记录区**写明改了什么、为什么。可参考 `WouoUI_graph.c` 里已有的登记格式。

`OLED*.c/h` 与 `Key.c` 改编自「江协科技」教学库，上游未声明许可，情况特殊，见 [`README.md` §14](README.md)。

---

## 四、提交与 PR

### 提交信息

沿用现有的中文 `type: 描述` 格式：

```
feat: 新增 XXX 模式
fix: 修正长周期 PWM 极性反相
docs: 补充 HARDWARE.md 电源拓扑说明
chore: 清理根目录临时产物
refactor: 统一引脚宏命名
```

**正文写「为什么」而不是「改了什么」**——`git diff` 已经说明了改什么。如果这个改动源自一次实测或一次踩坑，把测量条件与结论写进去，这比代码本身更有价值（README §10「踩过的坑」就是这么攒出来的）。

### PR 前自查

- [ ] `cmake --preset Debug && cmake --build build/Debug` 通过，**零告警**
- [ ] 构建末尾的 14 项 CubeMX 不变量检查全部通过
- [ ] 若改了 HRTIM / 时序 / 保护逻辑：**附上示波器实测波形**，并说明测量条件
- [ ] 若改了 `HARDWARE.md` 描述的引脚或电气特性：代码与文档同步更新
- [ ] 若新增 `.c` 文件：已加进根 `CMakeLists.txt` 的 `add_executable()` 列表

### 关于实机验证

固件改动**最好有实机验证**。测试项与判据见 [`README.md` §6](README.md)——至少覆盖你改动的那一块（比如动了互补输出，就测死区与是否直通）。

没有实机条件的 PR 也可以提，但请**在描述里明确说明未做实测**，不要让人误以为已验证过。

---

## 五、特别欢迎的贡献

按价值排序：

1. **文档纠错。** 代码与 `HARDWARE.md` / `README.md` 不符、数据过期、链接失效——这类问题在项目里最容易被忽视，也最影响别人使用。
2. **安全逻辑的审查。** 如果你是做功率电子的，请重点看 [`README.md` §6](README.md) 的三层保护。**指出一个防护漏洞比加十个功能有价值。**
3. **可移植性改进。** 目前工程强绑定这块板子（引脚、通道映射、OLED 型号）。如果你把它跑在了别的 G4 板子上，欢迎分享需要改哪些地方。
4. **`cmake/check_regen_invariants.cmake` 的推广。** 这个「CubeMX 重新生成防线」的思路可以原样搬到任何 CubeMX 工程——如果你在别的项目里用了并发现了改进点，欢迎回馈。

## 六、不接受的改动

- 把 `Drivers/` 换成 submodule 或 "精简" HAL —— 会破坏 CubeMX 重新生成流程
- 为了消告警而加 `-Werror` 或放宽 `-Wall -Wextra` —— 本工程是 `-O3 -flto`，`-Werror` 会让任何一次工具链小版本升级直接卡死构建
- 删除 `volatile`、重排急停顺序、简化时序常量 —— 见第三节
- 把 `Core/Src/` 下第三方的 `WouoUI*` / `OLED*` 文件重命名为自有风格

---

有疑问请直接开 issue，不用客气。
