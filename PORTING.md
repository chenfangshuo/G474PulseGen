# 移植指南

把这套固件搬到另一块板子上要改什么。**先读第一节——它决定这事能不能做。**

---

## 1. 前提：目标芯片必须带 HRTIM

**STM32G4 系列里，只有 `STM32G474` / `STM32G484` 有 HRTIM1。**

以下型号**没有** HRTIM，移植无从谈起（不是难改，是根本没有这个外设）：

| 没有 HRTIM | 有 HRTIM |
|:---|:---|
| G431 / G441 / G471 / G473 / G483 / G491 / G4A1 | **G474 / G484** |

> 依据：本仓库 vendored 的 CMSIS 器件头中，只有 `stm32g474xx.h` / `stm32g484xx.h` /
> `stm32g414xx.h` 定义了 `HRTIM1_BASE`，其余型号头文件里 `HRTIM` 出现次数为 0。
> ST 也把 G474/G484 单独划为「G4x4 高分辨率定时器线」。

外设版本是同一代（HRTIM v2），所以换 G474↔G484 之间不需要改 HRTIM 相关代码。

---

## 2. 分两层看移植难度

### 2.1 借思路：容易

下面这些与具体板子无关，可以直接抄到任何 HRTIM 工程：

| 内容 | 位置 |
|:---|:---|
| 八档分频选档算法与阈值表 | `Core/Src/Pulse.c` 的 `Pulse_CalcPrescalerAndCounts` |
| 多脉冲的 CMP4 软件重触发 + 开销补偿**标定方法** | 同文件，及 README §4.4 |
| 死区计算、互补输出四事件顺序 | `Pulse_CompPWM_CalcDeadTime` / `Pulse_CompLPWM_SetPW` |
| 三层保护的设计与**顺序理由** | README §6，`Pulse_EmergencyStop` |
| CubeMX 重新生成防线 | `cmake/check_regen_invariants.cmake`（完全独立于硬件） |

补偿常量的**数值**不要照抄——它绑定优化等级与工具链，必须在你的工程里重新标定。

### 2.2 整包搬过去：工作量不小

主要卡在下面第 3 节那张表。其中最费神的是**「改了不报错」**那一类。

---

## 3. 必须修改的清单

### 3.1 ⚠ 改了不报错 —— 优先级最高

这几处改错了编译器**不会有任何提示**，只会表现为"波形整体偏了一个比例"或"偶发乱擦"，极难定位。

| # | 项目 | 位置 | 说明 |
|:--|:---|:---|:---|
| 1 | **时钟基准** | `Core/Inc/Pulse.h` 的 `PULSE_HRTIM_CLK_HZ` | 必须等于实际 `SYSCLK`（HRTIM 的 fHRTIM 直接取 SYSCLK）。**改时钟树必同步改这里** |
| 2 | **通道端口与引脚** | `Core/Inc/Pulse.h` 的 `HRT_CHx_GPIO_Port` / `HRT_CHx_Pin` | 引脚换到别的端口时，两行都要改。引脚宏改了忘改端口宏**不会报错** |
| 3 | **互补通道对** | `Core/Src/Pulse.c` 的 `s_comp_pin_map[]` | 必须是**同一个 Timer 的 Tx1/Tx2**。配错 = 上下桥直通 |

关于第 1 条的量化后果：`Pulse_CalcPrescalerAndCounts` 用 `时间的秒数 × 该档等效频率` 算计数值。若 `PULSE_HRTIM_CLK_HZ` 写成 170 MHz 而实际 SYSCLK 是 128 MHz，**所有输出时间会变成设定值的 170/128 ≈ 1.33 倍**。

> 好消息：分频档的**阈值**（11.8 / 23.8 / 47.9 µs …）是在 170 MHz 下标定的。若实际主频更低，等效频率也低，这些阈值会**偏保守**（提前切到低分辨率档），**不会溢出**——只是分辨率略有损失。所以主频 ≤ 170 MHz 时改宏是安全的。

### 3.2 需要改但不属高危

| # | 项目 | 位置 | 说明 |
|:--|:---|:---|:---|
| 4 | 故障输入脚 | `Pulse.h` 的 `HRTIM_FLT2_GPIO_Port`/`_Pin`；`Pulse.c` 的 `Pulse_Fault_Init()` | 还要改该函数里的 `__HAL_RCC_GPIOx_CLK_ENABLE()`（HAL 没有"按端口开时钟"的通用宏）。极性与滤波档位见 `HARDWARE.md` §5.3，**不得改动** |
| 5 | SYNC / 帧标记输出 | `Pulse.h` 的 `SYNC_OUT_*` / `FRAME_OUT_*` | Y7 / Y8 两路辅助信号 |
| 6 | 12V 负载开关 | `main.h` 的 `LOADSW_GPIO_Port` / `LOADSW_Pin` | 急停第一步就断它，接错等于没有第一层保护 |
| 7 | 电源有效性检测 | `main.h` 的 `PWR1_DT_*` / `PWR2_DT_*` | 注意反相特性（见该处注释） |
| 8 | 按键阵列 | `Key.h` 的 `KEY_*_GPIO_Port` / `KEY_*_Pin` | 7 个按键/编码器/触发脚 |
| 9 | 显示 | `Core/Src/OLED*.c`、`WouoUI_user.c` | 强绑 **128×128 SSD1315 + SPI1 + DMA**。换屏基本等于重写 UI 层 |
| 10 | 外设集合 | `G474PulseGen.ioc` | 见下表 |

**固件依赖的外设**（缺任何一个都不只是改配置的事）：

```
HRTIM1       发波核心 (Timer A/B/D 出波形, Timer C 出 SYNC)
SPI1 + DMA    OLED 刷屏
TIM2         编码器接口模式
TIM5         长脉冲系列 (软件翻转 GPIO)
TIM6 / TIM7  刷屏节拍 / 按键扫描
TIM16        状态刷新
USART3 + DMA SCPI 与屏幕镜像
EXTI         硬件触发键
GPIO         12V 开关、电源检测、帧标记
```

### 3.3 不需要改的

- **参数存储页**：`Preset.c` 的 `PRESET_FLASH_ADDR` 与页号都是**自动推导**的
  （`FLASH_BASE + FLASH_SIZE - FLASH_PAGE_SIZE`，页号取 `FLASH_PAGE_NB - 1`），
  128KB / 256KB / 512KB 都正确，无需改动。但**要确认链接脚本的 FLASH LENGTH 与实际容量一致**。
- **`Drivers/`**：HAL 与 CMSIS 已随仓库入库，无需 submodule。

---

## 4. 移植步骤

1. **确认芯片**：必须是 G474 / G484（见 §1）。
2. **用 CubeMX 建新工程**，按你的板子配好 §3.2 那套外设，时钟树尽量对齐（不对齐也行，但要改 §3.1 第 1 条）。
3. **搬 `Drivers/`、`cmake/`、`CMakeLists.txt`、`STM32G474XX_FLASH.ld`**（链接脚本按新容量调整 `LENGTH`）。
4. **搬自有代码**：`Core/Src` 与 `Core/Inc` 里除 CubeMX 生成文件外的部分。
   保留 `/* USER CODE BEGIN */ ... /* USER CODE END */` 的分区习惯。
5. **改 §3.1 的三处**，再改 §3.2 的宏。
6. **编译**。构建前置的 `check_regen_invariants.cmake` 会检查 `.ioc` 相关的不变量，
   报 FATAL 说明某项配置应写进 `.ioc` 而不是手改源码（详见 README §9.5）。
7. **重新标定** `NPULSE_INTERVAL_COMP_US`（`Pulse.c`）——它的数值绑定优化等级与工具链，
   照抄原值在你的工程里不一定准。标定方法见 README §4.4。
8. **`Key.c` 等 CubeMX 生成文件的归属**：若新工程也要用 CubeMX 重新生成，
   注意 `usart.c` 在本工程是**受 `.ioc` 管辖**的（见 README §9.5），别手改。

---

## 5. 实机验证清单

移植后**必须**逐项实测。这几项错了不会编译报错，只会炸驱动板。

| # | 项目 | 判据 |
|:--|:---|:---|
| 1 | **上电不输出** | 上电瞬间所有输出为无效电平，无寄生脉冲 |
| 2 | **各模式波形** | 7 种模式逐一验证脉宽/周期/间隔与设定一致 |
| 3 | **死区** | 互补模式逐档测死区，**确认任何一档都没有上下桥重叠** |
| 4 | **故障封锁** | 拉低故障脚 → 输出立即封锁 + 12V 切断 + UI 弹 Fault |
| 5 | **急停顺序** | 确认 `LOADSW_DISABLE()` 仍是第一条执行 |
| 6 | **SYNC 对齐** | 双通道测 SYNC 与首脉冲的抖动 |
| 7 | **多脉冲间隔** | 按 README §4.4 的方法重新标定并复测 |
| 8 | **12V 互锁** | 断开电源输入，12V_OUT 应在 1 个主循环周期内断开 |

> 示波器用 1× 探头 + 1 MΩ 输入即可，别用 50 Ω 档——本板输出不是 50 Ω 源端匹配的传输线环境。

---

## 6. 已知不可移植 / 需要重新设计的部分

- **模拟前端**：电平转换（74LVCH8T245）、SMA 输出端接、电源路径（LTC4421 理想二极管、
  TPS22810 负载开关）都是本板的硬件设计，换板需要自行设计。
- **UI**：128×128 SSD1315 与 WouoUI 框架强绑定。换分辨率或换屏需重写
  `WuoUI_user.c`（1556 行，UI 数据模型与页面回调）。
- **硬件文档**：本仓库不提供原理图、PCB 与 BOM，无法据此复刻板卡，见 README §12。

---

有移植相关的疑问或踩坑，欢迎开 issue —— 这份文档就是照着"哪些地方改错了不会报错"整理的，
如果你的经历补充了新的坑，请提 PR。
