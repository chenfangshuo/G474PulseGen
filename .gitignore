# ========================================================
# 1. CLion & CMake 编译构建缓存 (核心占用大户)
# ========================================================
build/
cmake-build-*/
Debug/
Release/
bin/
obj/

# ========================================================
# 2. 嵌入式编译二进制产物与生成文件
# ========================================================
*.o
*.obj
*.d
*.elf
*.hex
*.bin
*.axf
*.map
*.crf
*.lst

# ========================================================
# 3. CLion / JetBrains IDE 专属配置与缓存
# ========================================================
# 注意: 这里必须写 .idea/* 而不是 .idea/ —— git 的规则是"父目录被排除时,
# 无法再重新包含其下的文件", 写成 .idea/ 时下面的 ! 例外不会生效。
.idea/*
# 例外: 项目拼写词典值得入库 —— 它收录的是本工程的领域术语 (HRTIM/NPULSE/RXNE
# 等), 入库后重新克隆即可直接消除约 230 条拼写类告警噪音, 且不会掩盖真正的
# 拼写错误 (区别于"全局关闭拼写检查")。
!.idea/dictionaries/
!.idea/dictionaries/project.xml
# 例外: 项目级代码检查配置 —— 与词典同理, 它是"本工程的约定"而非个人偏好。
# 两个文件缺一不可:
#   G474PulseGen.xml       定义 profile 内容 (哪些检查被降级/关闭)
#   profiles_settings.xml  记录"当前激活哪个 profile"
# 只提交前者的话, 别人克隆后 profile 文件在、但没有任何 profile 被选中,
# 配置静默失效 —— 比不提交更难排查。
!.idea/inspectionProfiles/
!.idea/inspectionProfiles/G474PulseGen.xml
!.idea/inspectionProfiles/profiles_settings.xml
.vscode/
*.launch
*.uvoptx
*.uvguix.*
*.scvd
*.dep
*.bak
*.tmp
*.log
litellm.log

# ========================================================
# 4. 操作系统临时文件
# ========================================================
.DS_Store
Thumbs.db
desktop.ini
# ========================================================
# 5. Python 上位机 / PyInstaller 打包产物
# ========================================================
__pycache__/
*.pyc
*.spec
pc_host/build/
pc_host/dist/
# 注: screenshots/ 已取消忽略 —— README.md 需要引用这些实拍图/波形图,
# 推到 GitHub 后必须能正常渲染。
# 入库的图一律压成 8-bit 调色板 PNG (ffmpeg palettegen/paletteuse, dither=none):
# 示波器截图颜色本就稀疏, 2048x1200 的余晖图可从 7.04 MB 压到约 25 KB 而无可见
# 损失。2026-09 复核时发现两张 verification 余晖图漏压 (各 7 MB), 已补压,
# screenshots/ 由 14.63 MiB 降至 0.59 MiB。新增截图请沿用同一手法。

# ========================================================
# 6. Claude Code 配置
# ========================================================
# 用 .claude/* (而非 .claude/) : git 无法重新包含一个"父目录已被排除"的文件,
# 排除目录本身会让下面的 ! 白名单失效。排除其内容才能开例外。
.claude/*
# 例外: 实机验证清单页面 —— 它是交付物而非本机配置, 换台机器也要能打开。
# settings.json / settings.local.json 是本机配置, 保持忽略。
!.claude/checklist.html

# ========================================================
# 7. 代码库分析临时快照 (repomix / tree 导出)
# ========================================================
repomix-core.txt
tree.txt

# 重构静态验证的基线快照 (scripts/verify_refactor.sh 生成)
# 它绑定"你本机当前的构建产物", 换台机器或换工具链版本就失效, 故不入库。
.verify/

# ========================================================
# 8. 未参与构建的 vendored 驱动 (体积裁剪, 2026-09)
# ========================================================
# CubeMX 生成工程时会把整个 CMSIS pack 拷进来, 其中 DSP 与 NN 两个子库
# 本工程从未使用 —— 全仓对 arm_math.h / arm_nn 零引用, 根 CMakeLists.txt
# 只把 HAL_Inc / HAL_Inc_Legacy / CMSIS_Device_Inc / CMSIS_Include 四个目录
# 加进构建, 这两者不在其中。
#
# 它们此前被跟踪, 合计约 48 MiB, 占仓库总体积的一半以上 (其中仅 DSP/Lib 下
# 的 6 个 .a + 4 个 .lib 预编译库就有约 32 MB)。已 git rm --cached 移出索引。
#
# 磁盘上刻意保留: CubeMX 重新生成时可能需要它们, 且保留可随时撤销本决定。
# 经完整重新配置 + 全量重编译验证: 移除前后 RAM 28200 B / FLASH 137956 B
# 逐字节一致, 对构建产物无任何影响。
Drivers/CMSIS/DSP/
Drivers/CMSIS/NN/
